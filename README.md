# TSCSSL
Unsupervised ML can cluster hydrogeochemical data, but its results are often difficult to interpret when multiple controls produce many clusters. Reducing cluster numbers may improve clarity but increase intra-class variability. To resolve this without altering the original clustering, we developed TSCSSL.
